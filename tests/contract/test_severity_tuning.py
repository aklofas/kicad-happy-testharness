"""Severity tuning tests — RETIRED in v2.0 (spec §5).

severity_tuning.json, severity_tuning.schema.json, and _apply_severity_tuning()
were deleted as part of the Layer 2 cage decommission. This file is kept as a
tombstone so the deletion is visible in git history.
"""

# All tests in this file are intentionally deleted.
# They tested _apply_severity_tuning() and the severity_tuning.json matrix,
# both of which were removed in v2.0 (spec §5 — Layer 2 cage decommission).
